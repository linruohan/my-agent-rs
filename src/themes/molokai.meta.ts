import type { ThemeDefinition } from './types';

const theme: ThemeDefinition = {
  id: 'molokai',
  name: "Molokai",
  description: "Molokai — Monokai 高对比变体",
  preview: ["#fefaf9", "#a0a0ab", "#fefaf9"],
  source: 'gpui-component',
};

export default theme;
