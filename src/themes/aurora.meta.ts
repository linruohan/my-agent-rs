import type { ThemeDefinition } from './types';

const theme: ThemeDefinition = {
  id: 'aurora',
  name: "Aurora",
  description: "极光 — 北欧冷色（gpui 官方）",
  preview: ["#ffffff", "#0f172a", "#e2e8f0"],
  source: 'gpui-component',
};

export default theme;
